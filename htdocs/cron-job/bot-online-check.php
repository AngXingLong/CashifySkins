<?php
	/*require $_SERVER['DOCUMENT_ROOT']."/shared/redis.php";
	require "../shared/database.php";

	$unresponsive_bots = select("select steamid from bot where last_reported > date_sub(now(), interval 15 minute) ");
	foreach($unresponsive_bots[)
	$redis->publish("Bot Manager",$botsid);*/